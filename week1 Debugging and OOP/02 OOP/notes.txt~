to do:
- Cars, Pair and Time are old versions, look them again
- there was some tricky part with Time, but what exactly?
- a little awkward StackImpl... refactor it later;
  should toString() and length() be destructive?
- tasks 6 and 7: double linked list similar to above
  but there is factory method in 7*
- 
